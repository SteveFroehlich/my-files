
## Environment isolation via docker

Use docker containers plus mounts for isolation.
docker provides a tool for this called `sbx`

Installed via homebrew: 
```
brew install docker/tap/sbx
sbx login
```

Ok that won't work because my mac needs to be on mac OS 14.x.x or later. I am on 13.x.x
docs:  https://docs.docker.com/ai/sandboxes/
